# Dastonia

## Useful commands
### Build the website
`npm run build` (runs eleventy and outputs static site to _site)

### Launch dev server
`npm run serve` (runs eleventy --serve, builds and serves with live reload)
